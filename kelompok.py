class no_kelshift:

    def kelshift(self, kel, shift):
            self.kel = kel
            self.shift = shift
            return print(f"\nAnggota Kelompok {self.kel} Shift {self.shift} :\n")
